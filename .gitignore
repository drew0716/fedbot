# 🔐 Environment variables
.env

# 🧠 Model output and metadata (we want to track these now)
# Commented out so GitHub Actions can commit these files
# *.pkl
# *.index

# 📦 Python cache
__pycache__/
*.py[cod]
*.so

# 📂 Local raw crawl data (ignored)
about_the_fed_pages/

# Ignore chunks/ folder
chunks/
# BUT allow all files inside it to be tracked if added explicitly
!chunks/**
# Optional: ignore any temp files inside chunks/ that aren't .txt
chunks/*.tmp
chunks/*.bak

# 🚫 VSCode + Streamlit configs (safe to ignore)
.vscode/
.streamlit/config.toml
.streamlit/secrets.toml

# 📁 System files
.DS_Store
Thumbs.db
venv/
