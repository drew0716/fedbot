# 🔐 Environment variables
.env

# 🧠 Model output and metadata (we want to track these now)
# Commented out so GitHub Actions can commit these files
# *.pkl
# *.index

# 📦 Python cache
__pycache__/
*.py[cod]
*.so

# 📂 Local raw crawl data (ignored)
about_the_fed_pages/

# ✅ Keep chunks/ in repo but ignore untracked files unless explicitly added
# Allow GitHub Actions to commit chunk files
# This line ignores the folder by default
chunks/
# ...but this line *overrides* the ignore for committed content
!chunks/**
# Optional: ignore any temp files inside chunks/ that aren't .txt
chunks/*.tmp
chunks/*.bak

# 🚫 VSCode + Streamlit configs (safe to ignore)
.vscode/
.streamlit/config.toml

# 📁 System files
.DS_Store
Thumbs.db
venv/
